#include <stdio.h>

int main(int argc,char *args[]){

    printf("%u\n",(unsigned int)0);
    //0 - 1 在无符号整数运算中会下溢，结果是该类型的最大值。
    /*
      ‌类型提升规则‌
        当表达式中存在无符号类型（如unsigned int）时，其他操作数（如1）会隐式转换为无符号类型参与运算
        1。即使对无符号数使用负号（如-1），结果仍保持无符号类型
        1。

        ‌运算过程的类型一致性‌
        减法操作的两个操作数需保持类型一致。(unsigned int)0显式指定了无符号类型，而字面量1会隐式转换为unsigned int，因此整个表达式结果为无符号类型
        3
        4。

        ‌补码运算的二进制解释‌
        无符号数的减法通过补码加法实现。0 - 1等价于0 + (-1)，而-1的补码（全1二进制）被解释为无符号数的最大值（如32位下为4294967295）
        5
        6。

        关键区别：若表达式为0 - 1（无类型强制转换），结果会是int类型的-1；但显式指定unsigned int后，运算全程按无符号规则处理

    */
    printf("%u\n",(unsigned int)0 - 1);
    printf("%u\n",(unsigned int)-1);
}